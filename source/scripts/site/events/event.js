var site = site || {}
;

el.core.utils.createNamespace(site, 'events');

site.events.event = {

  'IMAGE_LOADED'            : 'event.image.loaded',

  'OVERLAY_SHOW_REQUEST'    : 'event.overlay.show.request',
  'OVERLAY_HIDE_REQUEST'    : 'event.overlay.hide.request'
};
